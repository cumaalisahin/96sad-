$(function(){$.getJSON(`https://sybraxdesign.fr/api/github/${id}/infos.json`,function(data){versionUpdate(data);});function versionUpdate(n){if(version<n.version){$("body").append(function(){const div=document.createElement("div");div.classList.add("updateVersion");div.setAttribute("onClick","this.remove()");div.innerHTML=`<p>The version you are testing is not up to date (${version}), the latest version (${n.version}) is available on <a target="_blank" href="https://github.com/${n.owner}/${n.name}">github</a>.</p>`;return div;});}else if(version>n.version){$("body").append(function(){const div=document.createElement("div");div.classList.add("updateVersion");div.setAttribute("onClick","this.remove()");div.innerHTML=`<p>The version you are testing has a version that does not exist (${version}). The latest official version (${n.version}) is available on <a target="_blank" href="https://github.com/${n.owner}/${n.name}">github</a>.</p>`;return div;});}}});